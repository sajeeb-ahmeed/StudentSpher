# Student Results System Environment Variables

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/student_results_db

# Alternative Database Connection Variables (if not using DATABASE_URL)
PGUSER=your_database_username
PGPASSWORD=your_database_password
PGHOST=localhost
PGPORT=5432
PGDATABASE=student_results_db

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-min-32-chars

# Server Configuration
PORT=5000
NODE_ENV=development

# For Production Deployment on Render/Railway/Vercel
# NODE_ENV=production
# PORT=10000

# For Neon Database (Production)
# DATABASE_URL=postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/student_results_db?sslmode=require

# Optional: File Upload Configuration (if implementing file uploads)
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=pdf,doc,docx,txt,jpg,png,zip

# Optional: Email Configuration (if implementing email features)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: Cloud Storage (if implementing file storage)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_BUCKET_NAME=student-results-files

# Security Headers (for production)
TRUST_PROXY=1